debug = 0;
menu = 0;
audio_play_sound(SOsampleSong, 2, 1);

cam = view_camera[0];
follow = Oplayer;
view_w_half = camera_get_view_width(cam)/2;
view_h_half = camera_get_view_height(cam)/2;
Xto = xstart;
Yto = ystart;

